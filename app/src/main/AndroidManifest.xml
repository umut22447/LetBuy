<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication5">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service
            android:name=".NotificationIntentService"
            android:exported="false"></service>

        <activity android:name=".UserInventoryActivity" />
        <activity android:name=".OfferDetailActivity" />
        <activity android:name=".PrepareOfferActivity" />
        <activity android:name=".ProfileUserModification" />
        <activity android:name=".UserOffersActivity" />
        <activity
            android:name=".ItemAddActivity"
            android:parentActivityName=".MainMenuActivity" />
        <activity
            android:name=".ItemDetailActivity"
            android:parentActivityName=".MainMenuActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity
            android:name=".MyProfileActivity"
            android:parentActivityName=".MainMenuActivity" />
        <activity android:name=".RegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain"/>
                <data android:mimeType="image/*"/>
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>